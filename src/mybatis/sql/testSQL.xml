<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="testSQL">


    <select id="getTestOne" resultType="org.walkframework.data.util.DataMap">
        select * from comment where comment_id=#{id}
    </select>
    <select id="getTestList" resultType="org.walkframework.data.util.DataMap">
        select * from comment where comment_id>5
    </select>
</mapper>